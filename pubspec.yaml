name: flanb
description: Build Flutter APKs and share them over your local network.
version: 0.1.0

environment:
  sdk: '>=3.0.0 <4.0.0'

executables:
  flanb: flanb

dependencies:
  args: ^2.7.0
  path: ^1.9.0
  shelf: ^1.4.1
  shelf_router: ^1.1.4
  yaml: ^3.1.2

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.6

